"""Telegram notification for the value module (phase 8).

Alerts route attention; they do not decide. Phases 4b and 6 closed the automated
entry decision, and nothing here reopens it: the message fires on arithmetic the
screen already computes, names no action, and the only thing that writes to the
decision log is a human typing a command.
"""
