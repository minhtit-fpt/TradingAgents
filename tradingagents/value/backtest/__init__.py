"""Phase 3: the free numeric backtest.

Replays the screen over a decade of point-in-time facts and asks the only
question that justifies phase 5 onwards: does this criteria set actually select
outperformers? Costs nothing in LLM tokens, so it can be re-run for every
threshold change.
"""
